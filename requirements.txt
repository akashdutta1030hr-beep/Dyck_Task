# Dyck Task - Python 3.10+
# pip install -r requirements.txt

# Core
torch>=2.0.0
transformers>=4.35.0

# Training (Unsloth + LoRA)
unsloth[colab-new]>=2024.1
bitsandbytes>=0.41.0
peft>=0.6.0
accelerate>=0.24.0

# Data
datasets>=2.14.0
numpy>=1.24.0

# Training utils
matplotlib>=3.7.0

# Inference uses transformers + peft (no vLLM)
